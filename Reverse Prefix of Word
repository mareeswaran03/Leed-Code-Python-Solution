class Solution:
    def reversePrefix(self, word: str, ch: str) -> str:
        ans=""
        for i in range(len(word)):
            if word[i]==ch:
                break
            else:
                i=0
    
        return word[:i+1][::-1]+word[i+1:]
