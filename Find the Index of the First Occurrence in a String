class Solution(object):
    def strStr(self, haystack, needle):
        if needle in haystack :
            index=haystack.find(needle)
        else:
            index=-1 
        return index
        
